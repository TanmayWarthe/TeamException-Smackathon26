// src/services/mockData.ts
// DEPRECATED: Mock data fallbacks have been removed in favor of live dynamic API responses.
// Re-export type definitions from src/types to preserve backwards compatibility.

export type {
  Threat,
  DigitalTwin,
  AppNotification,
  DashboardStats,
  TimelineItem,
  StatisticsData,
  ProtectionEventStats,
} from '../types';